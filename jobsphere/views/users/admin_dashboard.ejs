<style>
    :root {
        --primary-bg: #1d1f26;
        --secondary-bg: #121417;
        --primary-text: #e0e0e0;
        --secondary-text: #b0b0b0;
        --accent-color: #04fcfc;
        --card-bg: #2c2f38;
        --card-shadow: rgba(0, 0, 0, 0.2);
        --card-shadow-hover: rgba(0, 0, 0, 0.25);
        --border-radius: 1rem;
    }

    body {
        background-color: var(--primary-bg);
        color: var(--primary-text);
        font-family: 'Poppins', sans-serif;
    }

    .dashboard {
        max-width: 1200px;
        margin: 0 auto;
        padding: 2rem;
        background-color: var(--secondary-bg);
        border-radius: 1.5rem;
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
    }

    .dashboard-header {
        margin-bottom: 2rem;
        text-align: center;
    }

    .dashboard-header h1 {
        font-size: 2.5rem;
        font-weight: 700;
        color: var(--primary-text);
        margin-bottom: 0.5rem;
        text-transform: uppercase;
        letter-spacing: 2px;
    }

    .dashboard-header p {
        font-size: 1.25rem;
        color: var(--secondary-text);
        opacity: 0.9;
    }

    .dashboard-grid {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
        gap: 1.5rem;
    }

    .dashboard-card {
        background-color: var(--card-bg);
        border-radius: var(--border-radius);
        padding: 1.5rem;
        box-shadow: 0 4px 6px var(--card-shadow);
        transition: all 0.3s ease;
        border: 1px solid rgba(255, 255, 255, 0.1);
    }

    .dashboard-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 10px 15px var(--card-shadow-hover);
        border-color: var(--accent-color);
    }

    .dashboard-card h2 {
        font-size: 1.5rem;
        font-weight: 600;
        color: var(--accent-color);
        margin-bottom: 1rem;
        border-bottom: 2px solid var(--accent-color);
        padding-bottom: 0.5rem;
    }

    .dashboard-card p {
        color: var(--secondary-text);
        margin-bottom: 1rem;
        opacity: 0.8;
    }

    .dashboard-card a {
        color: var(--accent-color);
        text-decoration: none;
        font-weight: 500;
        transition: color 0.3s ease;
        display: inline-block;
        padding: 0.5rem 1rem;
        border: 1px solid var(--accent-color);
        border-radius: 2rem;
    }

    .dashboard-card a:hover {
        background-color: var(--accent-color);
        color: var(--primary-bg);
    }

    @media (max-width: 768px) {
        .dashboard-header h1 {
            font-size: 2rem;
        }

        .dashboard-header p {
            font-size: 1rem;
        }

        .dashboard-card h2 {
            font-size: 1.25rem;
        }
    }
</style>

<% if (successMessage.length > 0) { %>
    <div class="flash-message success">
        <%= successMessage %>
    </div>
<% } %>
<% if (errorMessage.length) { %>
    <div class="flash-message error">
        <%= errorMessage %>
    </div>
<% } %>
  
<div class="dashboard">
    <div class="dashboard-header">
        <h1>Admin Dashboard</h1>
        <p>Welcome back! Here's an overview of your admin activities.</p>
    </div>
    <div class="dashboard-grid">
        <div class="dashboard-card">
            <h2>Analytics</h2>
            <p>Insights into user activity and job postings.</p>
            <a href="/dashboard/stats" class="btn btn-outline-primary">View analytics</a>
        </div>
        <div class="dashboard-card">
            <h2>User Management</h2>
            <p>Manage registered users on the platform.</p>
            <a href="/admin/users" class="btn btn-outline-primary">View users</a>
        </div>
        <div class="dashboard-card">
            <h2>Job Listings</h2>
            <p>Manage jobs on this platform.</p>
            <a href="/admin/job_management/all" class="btn btn-outline-primary">View job listings</a>
        </div>
        <div class="dashboard-card">
            <h2>Applications Overview</h2>
            <p>Top 10 trending jobs detail.</p>
            <a href="/admin/top_trending_jobs" class="btn btn-outline-primary">View applications</a>
        </div>
    </div>
</div>
